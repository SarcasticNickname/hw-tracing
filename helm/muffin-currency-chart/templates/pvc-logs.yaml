{{- if .Values.logs.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "muffin-currency.fullname" . }}-logs
  labels:
    {{- include "muffin-currency.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.logs.pvc.size | quote }}
  {{- if .Values.logs.pvc.storageClassName }}
  storageClassName: {{ .Values.logs.pvc.storageClassName | quote }}
  {{- end }}
{{- end }}
